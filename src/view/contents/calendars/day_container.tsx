import React, { ReactElement } from 'react';
import { Col } from 'antd';

const DayContainer = (): ReactElement => {
  return <Col style={{ border: '1px solid black' }}>day 1</Col>;
};

export default DayContainer;
